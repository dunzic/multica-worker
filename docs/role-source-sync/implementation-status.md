# Implementation status

Updated: 2026-08-13

| Feature | State | Current evidence | Next completion evidence |
| --- | --- | --- | --- |
| RS-01 Adapter registry and source registration | In progress | Source-neutral descriptor catalog keeps filesystem authority out of the server; authenticated member/admin APIs, role authorization, default-off dual flags, safe response DTOs, strict request decoding, typed bounded config summary, source persistence/control-plane registration transaction; explicit no-FK workspace teardown protected by workspace mutation locks; runtime/profile/stale cleanup cannot orphan a live-bound source and legacy runtime consolidation rebinds live sources before old-row deletion; controlled CAS pause/resume/detach/rebind uses one lock order, atomically cancels in-flight scans, clears unconsumed secret transfers, preserves history, releases detached runtimes and requires fresh matching loaded evidence before resume; rebind uses a guided current-workspace runtime picker with alias/provider and online/offline status rather than raw UUID entry while leaving the server's workspace lock and resume attestation as the authority; the settings card exposes the newest 100 lifecycle events through a closed member-readable projection containing transition, actor, time, sequence, commitment, cancellation counts and rebind runtime IDs, recomputes every returned event commitment, uses a lifecycle-only partial listing index and never returns the generic audit payload or plan/receipt data; AgentWaker, direct normalized-manifest directory and Ed25519-pinned remote HTTPS adapters use the same compile-time contract and daemon registry; the remote adapter has same-origin/SSRF/redirect/size fences, a three-key rotation set and typed issuer/key/revision/signature evidence; human-only managed local config supports redacted recovery summary, mandatory CAS desired-state apply, atomic private publish, source add/remove and explicit key rotation; five-second secure hot reload atomically publishes a fully validated scanner generation, isolates in-flight work, retains the last-known-good generation on invalid/missing/insecure replacement, renegotiates runtime-scoped evidence and reports secret-free local health; daemon-loaded configuration is attested with bounded runtime-scoped config-ID digests plus kind/version, durably acknowledged once per runtime/config state and exposed as drift/history; current status also uses Redis liveness or the sweeper's shared 150-second DB fallback so historical loaded evidence is not shown as live after runtime loss; a fixed-cardinality fleet gauge, sustained-unavailable Helm alert and operator recovery runbook provide proactive freshness operations; the candidate image includes a strictly read-only PostgreSQL 17 probe that exercises the exact source/attestation/runtime reads concurrently, checks required indexes and emits a private identifier-free percentile report bound to the build commit | hardware-backed publisher operation/transparency or anti-replay policy, live remote-origin and cross-platform hot-reload fault evidence, and candidate 10,000-user read/write/API/S3/failover results |
| RS-02 Bounded scan and contract validation | In progress | Hardened root-confined filesystem with root identity recheck; AgentWaker normalization, strict JSON normalized-manifest directory and signed remote adapters; the remote path verifies domain-separated Ed25519 manifest/tree commitments before fetching digest-bound artifacts through a public-address-only bounded HTTPS client; canonical snapshot identity; generic manifest-bomb limits; managed private daemon config with manager-owned digest key, allowed-root policy, profile discovery, non-writable canonical parent, cross-process lock, atomic replace and generation-isolated hot reload; durable per-source scan queue with expiring/renewable leases; explicit capability/poll negotiation; push wakeup plus throttled recovery poll; bounded daemon concurrency; stale-owner rejection; retrying idempotent terminal report API; the settings surface lets owner/admin queue only a read-only scan, restores the latest redacted status after refresh, polls only queued/claimed work and exposes stable terminal codes while members retain read access; a separate member-readable timeline lists at most 100 recent scans with status/error/snapshot/adapter/time while redacting requester, runtime, lease and request-key evidence, filters that bounded set by status or stable-code substring and maps trust/runtime/source/transient families to conservative non-mutating recovery guidance; approval/apply remain absent; every adapter reopens artifacts and revalidates exact digest/size; missing-only upload uses bounded batches, a private spool, server-side SHA-256 verification, deterministic tenant-scoped object keys and an immutable readiness ledger; snapshot acceptance locks readiness and integrity rows and persists exact digest/size reachability edges in the same transaction; a separate leased integrity state machine recomputes stored size/SHA-256, quarantines confirmed missing or mismatched bodies, retries transient reads, excludes checking/quarantined bodies from snapshot and apply, and repairs only through an exact re-upload; a leased deletion-intent reconciler reclaims 24-hour unreachable/workspace-deleted objects with backoff, revive fencing and a widening late-PUT tombstone tail; snapshots with unavailable artifact bodies are rejected | guided config UI, server-side fleet search and explicit incident correlation, large/binary direct-upload protocol, richer stable failure taxonomy, source-change retry, live readback/mutation/re-upload race evidence, live DNS/TLS/CDN/key-rotation/replay evidence and PostgreSQL GC/lease/concurrency/failover evidence |
| RS-03 Snapshot, diff, approval and atomic apply | In progress | Deterministic plans are generated only from persisted, digest-revalidated snapshots; member evidence and admin plan/approval/apply APIs are tenant-scoped and separately default-off; every archive candidate requires an explicit archive/retain decision; large destructive reviews render only 50 canonical candidates per page, preserve the complete decision map and expose page-scoped bulk choices without enabling approval until the full plan is exhaustive; approved apply revalidates the current-snapshot CAS and exact approval, serializes the source, commits domain writes/current snapshot/idempotent apply receipt/hash-chained audit in one transaction, and verifies stored receipt digests on retry; a COMMIT response error is independently reconciled by exact idempotency key and only a digest/actor/approval/secret-transfer-exact receipt converts it to success; other errors after safe request identity is established write an independent append-only, content-free failed-attempt row; the operator surface maps stable failure codes to reconcile/secret-refresh/dependency-recovery/rescan guidance, refreshes current source/scan/plan/receipt/failure evidence and links only current-plan failures back to normal guarded apply—never exposing a blind historical retry; the shared Web/Desktop settings surface remains read-only unless the independent apply flag is enabled, then lets owner/admin generate a plan only from the latest successful snapshot, make exhaustive canonical archive/retain decisions, bind an approval to the exact digest, pass a second confirmation before apply and inspect successful content-addressed receipts; ambiguous approval/apply responses retain the same idempotency key and malformed responses fail closed | live failure-at-every-step injection, durable fallback when PostgreSQL itself is unavailable, outbox/after-commit events, retention/partitioning, conflict/adoption decisions, live PostgreSQL duplicate-key/commit-timeout/restart proof, large-plan search/export/accessibility usability evidence and recorded controlled recovery rehearsal |
| RS-04 Materialization | In progress | Stable source-object mappings and ownership masks; roles become private runtime-bound agents; skill supporting files use per-path ownership; read-only capability bindings materialize deterministic marker/package namespaces into their exact target skill and archive without touching user files; schedule automations remain paused with disabled deterministic triggers; immutable capability versions and mapping/file writes retain tenant and digest checks; the final provenance mapping ledger now flushes in canonical exact-return-set batches bounded by 500 rows and 8 MiB instead of one unbounded apply-wide statement; deterministic tenant-validated employee-to-skill association batches are bounded by 1,000 rows and 2 MiB, verify exact returned pairs and do not re-enable user-disabled bindings; one validated capability-ID index removes repeated linear searches and quadratic CPU risk at the 10,000-capability/binding limit; one bounded tenant name preflight replaces per-object conflict queries and rejects plan-internal duplicates; immutable capability definitions persist in exact-match batches bounded by 500 rows and 2 MiB; role-agent, Skill main-table, supporting-file final-state mutations and Autopilot-plus-trigger creates/updates use canonical exact-return-set batches bounded by 250 rows and 64 MiB while preserving user-controlled fields; affected existing Skill rows are locked canonically and only the bounded desired-or-owned file paths are loaded/locked through an exact tenant-scoped target set, transaction-local state preserves cross-object path ownership and collapses each path to one insert/update/delete, and inserts have no conflict overwrite path so a concurrent user-created file fails the whole apply; slow artifact verification remains outside mutation locks | profile target contract, hard local/external-write permission boundary, executable adapter binding, reversible skill/capability removal, explicit adoption, database-enforced active-autopilot title race policy, live 1,000-role/10,000-skill PostgreSQL SLO, cross-runtime tests and affected-worker UI |
| RS-05 Secret and MCP synchronization | In progress | Plaintext-in-snapshot flaw rejected; independent daemon capability negotiates short-lived one-time public-key challenges; source re-scan and HMAC/hash checks bind encrypted environment/MCP payloads to the exact approved snapshot; persisted ephemeral private keys use versioned at-rest AEAD with current/previous key rotation; plans expose only a non-sensitive per-role transfer requirement and reject more transfer roles than one bounded apply can accept; a plan-bound member-readable configuration review projects at most 100 changed environment keys and MCP service IDs with role, object type and create/update/archive status while structurally excluding values, value digests, definitions, URLs, commands, arguments and headers; the operator UI requests challenges only after exact approval, polls a redacted latest-status view, never exposes challenge keys/claims/ciphertext and keeps apply disabled until every unexpired role transfer is submitted; apply sends the exact role-to-transfer-ID map, preserves user-owned keys, consumes ciphertext atomically, records receipt/audit evidence, and clears consumed/expired recoverable transfer material | live PostgreSQL request/claim/submit/apply/consume and expiry-sweeper proof, operator key/runbook validation, restart/failover/load/exfiltration exercises |
| RS-06 Versioning, provenance and rollback | In progress | Immutable snapshots and apply receipts form the provenance ledger; rollback creates a digest-distinct forward plan only to a previously successful receipt-validated snapshot; the controlled owner/admin UI exposes eligible historical receipts, suppresses the active snapshot, adds a proposal-only confirmation and visibly labels the resulting forward plan before reusing impact review, exact approval, secret-transfer and apply confirmation gates; every source-managed task captures a content-free source/role/snapshot/target-state/capability pin; old daemons cannot claim capability-pinned work; the current daemon verifies the bound target skill, exact marker, resolved version, permission and every package file digest before execution; the plan audit view separates mandatory pre-start cancellation, archive-conditional cancellation and running work that continues on its captured version; an indexed, backfilled snapshot-to-artifact edge ledger makes reachability explicit and transactionally fenced; unreferenced and workspace-deleted object bodies enter a durable leased deletion/tombstone state machine instead of disappearing from the database first; owner-only append-only source/snapshot legal holds use closed reason codes and digest-only external references, are hash-chain audited, database mutation guarded and hard-fence workspace deletion; append-only retention-policy revisions drive a bounded leased candidate/prune state machine that rechecks current state, holds, task pins, mappings, active transfer/apply, recent/approved plans and latest successful rollback reserves in the deletion transaction; task-pin/snapshot database locks close the insertion-delete race; the owner UI provides exact-count/byte preview and policy CAS without a manual-delete bypass; the worker is independently default-off behind permanent artifact GC with low-cardinality backlog/failure alerts; the operator DR tool now exports one PostgreSQL snapshot plus every byte-verified reachable artifact under an exclusive/shared destructive lock, records content-free commitments and active key IDs, supports idempotent object rehydration, and verifies snapshots/plans/receipts/audit chains/cross-table edges/objects/decryptability before traffic | live PostgreSQL hold/pin/policy/delete/failover races and EXPLAIN/SLO evidence, projected uniquely reclaimable bytes and realized savings, policy approval/RACI, old-version encrypted-config reconstruction or re-enqueue UX, paged rich version comparison, write/adapter authority, cross-runtime proof, and a recorded Gate F restore exercise with RPO/RTO |
| RS-07 Delivery receipts and external readback | In progress, two-connector backend pilot | Content-free connector-neutral ledger with task/installation idempotency, expiring send leases, crash reconciliation, stable correlation IDs, provider message IDs and digest-verified evidence; Slack and DingTalk chat completions use the same contract; an explicit user reply monotonically advances `delivered` to `readback`; workspace members can list verified evidence without message bodies; the Integrations UI shows the newest 100 verified rows with status filtering, connector/task/correlation/attempt/safe-error/evidence/time fields, suppresses routing IDs, explicitly distinguishes reply evidence from passive read receipts and provides no unsafe blind resend action | Feishu/WeCom adoption, provider-supported idempotency or ambiguous-send reconciliation, attachment authorization receipts, controlled operator retry after reconciliation, failure notification, live callback/outage/load proof and production telemetry |

## Current branch quality evidence

- `go test ./internal/rolesource`
- `go test ./internal/rolesource/...`
- `go test ./internal/handler` (includes role-source API/feature-flag/redaction/protocol tests)
- `go test -race ./internal/rolesource/...`
- targeted `go test -race` for role-source handler and adapter packages
- daemon composition tests for private config permissions, symlink/out-of-root rejection, generic AgentWaker execution, negotiation/poll throttling, lease/result wire bodies and bounded capacity
- the second `multica_manifest_directory` adapter passes the same generic descriptor/registry/snapshot/artifact-open path as AgentWaker, rejects unknown config fields, traversal, changed bodies, binary media and secret/MCP authority it does not advertise, and is wired into both server catalog and daemon config loading; a 10,000-role in-process scan fixture and bounded parser fuzz pass, which do not substitute for live upload/database SLO evidence
- the third `multica_signed_remote` adapter passes the same path without filesystem roots; tests cover strict same-origin HTTPS config, private-address/IP-literal/redirect boundary construction, canonical manifest/tree commitments, Ed25519 tamper and wrong/retired-key rejection, two-key staged rotation, changed artifacts, authority rejection, redacted trust summaries, cancellation and closed content-free `remote_unavailable`/`remote_trust_invalid`/`remote_content_invalid` terminal codes; live DNS rebinding, TLS/CDN outage, hardware signer and replay-policy evidence remain open
- managed daemon configuration tests cover first create, profile discovery, source add/remove, key generation/preservation/explicit rotation, malformed-state recovery, stale revision, active lock, symlink/public-file/out-of-root rejection, injected atomic-replace failure, terminal-safe redaction and daemon-task refusal; Windows packages cross-compile, but Windows locking/replacement still lacks a live runtime fault test
- hot-reload tests cover previously absent default enablement, valid generation swap, attestation/poll reset, same-revision recovery, invalid/deleted-file last-known-good retention, captured-generation semaphore release, secret-free health serialization and concurrent reload/heartbeat access under the race detector; live Windows, network-filesystem and process-kill exercises remain open
- runtime-loaded configuration evidence tests cover runtime-scoped private-body/config-ID commitments, sorted adapter identities, tamper/unknown-field rejection, unloaded versus unattested state, every config/kind/version drift classification, acknowledgement suppression/retry, bounded history, low-cardinality metrics/alerting, shared workspace/runtime lock ordering and runtime/workspace teardown contracts; opt-in PostgreSQL tests now encode duplicate restart history and registration/delete/heartbeat races, and an opt-in S3-compatible probe verifies upload/read/delete, but both are locally skipped and still require recorded staging/failover evidence
- content-addressed artifact tests for canonical reference collection, post-scan source-change rejection, preflight/upload wire metadata, server hash/size gates and snapshot readiness checks
- artifact-integrity migrations and tests cover transactional upload enrollment, leased bounded readback, exact size/SHA-256 verification, typed missing-object classification, transient retry without quarantine, fail-closed checking/quarantined readiness, exact re-upload repair accounting, workspace/GC cleanup, DR-table invariants, low-cardinality metrics and default-off Helm alerts; live versioned-object mutation, PostgreSQL lock races and fleet repair remain open
- snapshot artifact reachability migrations cover an explicit no-FK edge table, concurrent uniqueness/lookup indexes and exact-path historical backfill; scan success now locks readiness rows `FOR SHARE`, inserts and re-reads all canonical edges before completing the request, and workspace teardown explicitly removes edges before history disappears; live PostgreSQL lock-race and backfill execution remain open
- artifact deletion-intent migrations and static contracts cover unreachable atomic move, workspace-delete enqueue-before-row-removal, `SKIP LOCKED` leased claims, retry ownership tokens, cancellable pending/tombstone revive, 24-hour settle delay and four-pass late-PUT re-delete; role-source GC now requires permanent-purge support and S3 removes every retained object version/delete marker instead of treating a versioned-bucket delete marker as erasure; the server starts the worker only with object storage and the independent default-off `MULTICA_ROLE_SOURCE_ARTIFACT_GC_ENABLED` gate, exports bounded metrics and warns on sustained purge failures; live PostgreSQL/storage crash, timeout, Object Lock and revival races remain open
- legal-hold tests cover strict owner-only APIs, closed scope/reason schemas, digest-only request/external references, exact idempotent create/release, conflicting retries, source/snapshot validation, hash-chain event counts, active workspace-delete rejection, immutable-row database triggers, explicit teardown ordering and a settings surface hidden from admins; live PostgreSQL migration/trigger/concurrency evidence is encoded but skipped locally
- historical-retention tests cover conservative policy bounds, append-only CAS/idempotency, digest-only request persistence, exact preview totals with bounded details, every policy/hold/provenance/in-flight/rollback blocker, leased `SKIP LOCKED` candidates, deletion-authority and task-pin snapshot guards, capability reachability cleanup, audit completion, nested default-off worker gates, owner/admin UI separation and bounded telemetry/alerts; the opt-in PostgreSQL end-to-end hold-to-release-to-prune test is locally skipped
- full daemon and daemon WebSocket package tests pass with approved local-loopback access
- bounded fuzz runs for env parsing, path confinement and canonical JSON
- deterministic plan, snapshot-integrity, no-change and diagnostic-blocking tests
- plan/approval API tests for default-off behavior, deterministic response validation, exact idempotency-conflict mapping and idempotency-key redaction
- approval policy tests for blocked-plan rejection and exhaustive explicit archive/retain decisions
- apply contract tests for separate rollout gate, unsafe-scope 422 responses, stale snapshot CAS, tamper-evident receipts and ownership-contract blockers
- materialization SQL limits updates to declared source-owned agent/skill/autopilot fields; user status, enablement, secrets, MCP, model, permission and extra skill associations are preserved
- large-materialization transaction tests stage and canonicalize 10,000 role-to-skill bindings into tenant-validated batches bounded by 1,000 rows and 2 MiB, preserve a user-disabled existing association, fail closed if either endpoint is missing/cross-tenant/archived, and avoid two empty file-lock queries for each newly created skill without supporting files; live PostgreSQL statement size, WAL, lock wait and end-to-end SLO remain open
- materialization namespace/capability tests collect 10,000 names into one explicitly size-bounded tenant preflight, reject any non-mapped or plan-internal duplicate, and split 10,000 exact immutable capability definitions into count-and-byte-bounded batches; existing definition mismatch and partial returns fail the whole apply, while the historical autopilot table's lack of a title uniqueness constraint remains a live race objection
- role-agent materialization preallocates new target identities, batches create/update requests by count and encoded bytes, preserves permission/owner/lifecycle/model/secret/MCP/user fields on updates and rejects duplicate, missing or unexpected returned identities before mapping state advances; the 1,000-role fixture fits four bounded batches, while live PostgreSQL WAL/lock/latency evidence remains open
- Skill main-row materialization uses the same preallocated-ID/exact-return-set contract in count-and-byte-bounded batches; the 10,000-Skill fixture fits 40 batches, updates preserve config/creator, and source-owned supporting-file collision/removal/upsert checks still run before binding/mapping commit
- Skill supporting-file preflight locks only this apply's affected existing Skill targets in canonical UUID order and loads only the desired-or-owned paths through a deduplicated, sorted target set capped at 40,000 paths/16 MiB; unrelated user files are neither read nor locked; a transaction-local cache across Skill and capability objects collapses final per-path state into canonical insert/update/delete batches bounded by 250 rows and 64 MiB with exact target/operation return verification; inserts intentionally have no conflict handler, so a user path created after preflight aborts and rolls back instead of being overwritten; the 10,000-file shape fits 40 batches while live PostgreSQL race/lock/WAL/rollback evidence remains open
- schedule automation materialization batches Autopilot main rows and deterministic triggers together; only trigger-backed target IDs satisfy the exact-return-set check, new rules remain paused/run-only with disabled triggers, and updates preserve status/execution mode/assignee/enabled state; the 10,000-automation upper-bound fixture fits 40 batches, while the historical absence of a database active-title uniqueness constraint remains an explicit live race objection
- secret/MCP tests cover independent negotiation, source re-scan, encrypted daemon round trip, exact payload/hash validation, public-response redaction, key rotation, atomic-consumption contract and ciphertext clearing/expiry policy
- rollback tests cover digest-distinct forward plans, active-target rejection, receipt-backed historical eligibility, independent rollout gate and request-key-free apply history
- runtime-pin tests cover centralized enqueue capture, byte-stable retry inheritance, content-free typed wire evidence, target-state drift rejection, unchanged-mapping snapshot advancement, same-transaction invalidation of pre-start tasks, mapping-to-task finalization lock order and bounded composite-cursor history; the real PostgreSQL trigger behavior is encoded in an isolated-schema test but skipped locally because PostgreSQL is unavailable
- provider execution-environment tests preserve nested read-only capability markers and package entrypoints across every workspace-file provider plus Codex and Hermes task homes; these are filesystem contract tests, not live provider invocations
- settings UI tests prove the source/plan audit surface is hidden unless both sync and scan flags are enabled, renders current runtime loaded-config status, revision and historical evidence plus plan blockers, lets only owner/admin request a read-only scan, restores/polls its redacted state, and exposes no approval/apply action while the independent apply flag is off; apply-enabled tests bind plan generation to the latest successful snapshot, require exhaustive canonical archive decisions, require a second confirmation, reuse apply idempotency after an ambiguous response and show verified successful receipts
- plan-impact tests distinguish mandatory versus archive-conditional cancellation and running-version continuation; the read API uses a repeatable-read read-only transaction, bounded detail rows, aggregate counts and an active-task-only index without exposing task content
- failed-apply audit tests cover stable content-free classification/parameters, exact receipt matching and member-visible redaction; bounded Prometheus series distinguish returned errors, failure-evidence writes and commit-reconciliation outcomes without tenant/request labels, while Helm rules page on missing evidence or unresolved/conflicting commit checks; the UI warns that a remaining commit-stage record was not automatically confirmed; live PostgreSQL rollback ordering, database-outage fallback and commit-timeout reconciliation behavior remain unproven
- a deterministic 10,000-role snapshot/plan/validation test and allocation benchmark pass locally; this proves the in-process planning contract only, not database or end-to-end 10,000-user capacity
- a deterministic apply-preflight contract covers 1,000 roles plus 10,000 unique skill entrypoints at about 86 MiB, excludes unbound capability packages from apply memory, and proves object-storage reads precede mutation locks; it does not prove the live database write SLO
- delivery-ledger tests cover duplicate completion suppression, failed-attempt retry with stable correlation, out-of-order/duplicate readback, tamper rejection, payload-conflict fail-closed behavior and crash-lease expiry; Slack and DingTalk tests prove both real outbound adapters cross the shared claim/receipt boundary
- member delivery-history responses contain payload digests rather than message bodies and revalidate terminal evidence before serialization; workspace deletion now explicitly covers every role-source and channel-delivery table added in this work
- sqlc v1.31.1 generation; migration-policy tests; migration command compilation
- live PostgreSQL isolated-schema migration round-trip test exists but skipped locally because PostgreSQL was not running
- `git diff --check`
- official Helm 3.20.2 archive checksum verification, chart lint, default-off rendering, enabled capacity/backup Job rendering and fail-closed invalid-minimum/path cases
- repository-pinned pnpm 10.28.2 frontend verification: all six typecheck tasks and all five Vitest workspace tasks pass; ESLint completes with zero errors and pre-existing warnings
- `go vet ./...` passes; `go test ./...` passes every package except the pre-existing `pkg/agent` process-protocol suite, whose observed five-second timing failures reproduce in an isolated package run and pass when the same cases are serialized with `-parallel=1`; no role-source change touches that package

These checks cover the generic contract, bounded filesystem, AgentWaker adapter, authenticated API shapes, daemon-local execution, protocol negotiation/renewal, deterministic planning, artifact transfer, the static/unit behavior of atomic apply, content-free runtime provenance pins, read-only capability-package verification, and two connector delivery implementations. They do not prove live PostgreSQL migration/transaction failure behavior, lock contention/failover, provider callback behavior, cross-runtime capability execution or the 10,000-user production target.
